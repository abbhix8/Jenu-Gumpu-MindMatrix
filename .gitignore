# Custom .gitignore - Pushing everything allowed by GitHub
# Ignoring only files known to exceed 100MB limit
gradle-8.7-bin.zip

# Keeping some basic local-only files ignored for safety
local.properties
.DS_Store
*.log
*.tmp
